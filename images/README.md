# Screenshots

Drop PNG screenshots here with these exact names and they'll appear automatically
on the landing page (until a file exists, an elegant placeholder frame shows instead):

| File | Where to capture it |
|------|---------------------|
| `milestones.png` | The **Weekly Milestones** dashboard (`/milestones`) |
| `tracker.png`    | The **SE Execution Tracker** (`/tracker`) |
| `caf.png`        | A **CAF nomination packet** (Tracker → nominate → the packet view) |
| `connect.png`    | The **FY Connect** generator (`/connect`) |

Tips:
- Capture at a wide window (≈1400px) for crisp results.
- **Blur or crop out real customer names / ACR** before adding — this is a public page.
- Wide landscape shots look best in the framed layout.
